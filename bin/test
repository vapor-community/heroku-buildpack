#!/usr/bin/env bash
# bin/compile-test <build-dir> <cache-dir>

set -e
set -o pipefail

RUN_TESTS=1
BIN_DIR=$(cd $(dirname $0); pwd)

source "$BIN_DIR/compile"

puts-step "Migrating the database"

swift run Run migrate --yes

puts-step "Running test suite"

swift test
